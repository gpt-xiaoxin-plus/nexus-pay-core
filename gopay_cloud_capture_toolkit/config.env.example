# 复制为 config.env 后按你的云手机改。

# 云手机 ADB 地址，例如 localhost:7252 / 127.0.0.1:7252
ADB_SERIAL=127.0.0.1:7252

# mitmproxy 监听端口
PROXY_PORT=18080
MITMWEB_PORT=18081

# 云手机能访问到的 Mac IP。自动检测不准时手动填。
# 例：192.168.1.8
HOST_IP=

# GoPay/Gojek 包名可能随安装包不同变化，不确定可不填。
GOPAY_PACKAGE=

